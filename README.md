

## ✍️ Отредактированное и приведённое к формату задания

### **Задание**

Необходимо разработать модуль программного обеспечения для **управления библиотекой**, предназначенный для работы с базой данных и графическим интерфейсом пользователя.

Приложение должно обеспечивать удобную работу с данными о пользователях и бронировании книг и использоваться **администратором**, **руководителем** и **клиентами библиотеки**.
Реализация должна быть выполнена с использованием **PyQt (PyQt6)** и СУБД **MySQL**.

---

## 👤 Роли пользователей и функционал

### 🔐 **Авторизация**

В системе должна быть реализована:

* авторизация пользователей по логину и паролю;
* разграничение доступа в зависимости от роли пользователя (администратор, руководитель, клиент);
* открытие соответствующего окна после успешного входа.

---

## 🧑‍💼 **Администратор библиотеки**

Для администратора необходимо реализовать главное окно в виде **таблицы бронирования книг**, отображающей данные, получаемые из базы данных.

### В таблице должны отображаться следующие поля:

* Имя клиента
* Название книги
* Жанр
* Автор
* Дата выдачи
* Дата возврата
* Статус бронирования (например: *Забронировано*, *Выдано*, *Возвращено*)

### Функционал администратора:

* загрузка и отображение данных из базы данных в таблице;
* **фильтрация записей**:

  * по статусу бронирования;
  * и/или по периоду дат (дата выдачи / дата возврата);
* кнопка **«Показать все»** для сброса фильтрации;
* **поиск** записей (по имени клиента, названию книги или автору);
* **редактирование** выбранной записи;
* **удаление** записи с подтверждением действия;
* обновление данных в таблице после операций добавления, изменения и удаления;
* вывод сообщений пользователю (успех, ошибка, предупреждение) с использованием `QMessageBox`.

---

## 👤 **Клиент библиотеки**

Для клиента необходимо реализовать отдельный интерфейс.

### Функционал клиента:

* регистрация нового пользователя;
* авторизация в системе;
* просмотр **каталога книг**;
* выбор книги и указание периода бронирования;
* проверка доступности книги на выбранный период;
* бронирование (или взятие) книги при условии её доступности;
* личный кабинет, в котором отображаются:

  * текущие взятые / забронированные книги;
  * количество дней до окончания срока возврата;
  * история ранее взятых книг.

---

## 👔 **Руководитель библиотеки**

Для руководителя необходимо реализовать отдельное окно со **сводной информацией**.

### Функционал руководителя:

* просмотр краткой статистики:

  * общее количество книг;
  * количество выданных книг;
  * количество свободных книг;
  * количество активных бронирований;
  * ср. значение по периоду
* отображение статистических данных в удобной форме (таблица или текстовый блок).

---

## ⚙️ Общие требования к приложению

* все данные должны загружаться из базы данных;
* операции добавления, редактирования и удаления должны изменять данные в БД;
* интерфейс должен быть логичным и понятным;
* не допускается аварийное завершение приложения при ошибках;
* каждая роль пользователя должна иметь доступ **только к своему функционалу**;
* код должен быть структурирован по модулям (окна, работа с БД, логика).

---

### 💡 Почему это хорошо подходит под экзамен

* есть **таблица + БД + CRUD** (add / edit / delete);
* есть **фильтрация, поиск, сортировка**;
* есть **несколько ролей**;
* есть **реальный интерфейс на PyQt**;
* задание звучит официально и «по методичке», но не перегружено.

Если хочешь, следующим шагом могу:

* предложить **структуру БД под библиотеку** (таблицы + поля);
* показать, **как именно это лучше реализовать в PyQt (QTableView vs QTableWidget)**;
* или помочь переписать это задание **ещё ближе под формулировки твоего колледжа**.
